# To-Do

- [ ] Remove the parallax drift controls (Scale, Drift X, Drift Y sliders) from the Story block if the client decides they don't want the parallax animation
- [ ] **Pending business decision** — deprecate the iframe embed mode in the Highlights block (keep thumbnail-only mode)
- [ ] Remove the `margin-left: -0.3em` centering hack from `.heroSocial` in `frontend.css`
