{
  "$schema": "https://schemas.wp.org/trunk/block.json",
  "apiVersion": 3,
  "name": "the-black-cap/whats-on",
  "title": "What's On (Instagram)",
  "description": "Horizontal scroll slider of Instagram post embeds. Auto-fetches via Meta Graph API when credentials are set in Settings → Black Cap; falls back to manual shortcodes.",
  "category": "theme",
  "icon": "instagram",
  "supports": { "html": false },
  "attributes": {
    "shortcodes": {
      "type": "string",
      "default": "",
      "description": "Comma-separated Instagram post shortcodes used as fallback when no API token is configured."
    },
    "limit": { "type": "integer", "default": 8 }
  },
  "editorScript": "file:./index.js",
  "render": "file:./render.php"
}
